# PLAN.md — ブラウザ・バレーボールゲーム

## ゲームの目的
縦から見た3Dコートで、プレイヤー3人 vs CPU3人のラリーバレーボールを行う。
先に規定得点(5点)に到達した側が勝利する、短時間で遊べるカジュアルスポーツゲーム。

## プレイヤー操作
- プレイヤーは、任意の一人だけを操作できる。
- `←` / `→` / `↑` / `↓`: コート上を前後左右に移動
- `Space`: ジャンプ（3回目の返球=スパイクに必須）
- `S`: ボールを打つ/トスを上げる/レシーブする
- `Enter`: サーブ開始
- ボールに接触した状態でジャンプ入力すると、上方向＋相手コート方向へボールを打ち返す
- `R`: ゲームオーバー画面でリスタート

## メインループ
1. **タイトル画面**: タイトル表示 → 任意キーでスタート
2. **サーブ**: 得点した側（初回はランダム）が自陣からボールを軽く打ち上げてラリー開始
3. **ラリー**: 重力に従って落下するボールを、必ず3タッチで相手コートに返す（1・2回目はトス、3回目はジャンプ中のスパイクのみ、同じキャラの連続タッチは不可）
   - ボールが自陣の地面に着地 → 相手に1点
   - ボールがネットに当たって自陣側に落ちる → 相手に1点
4. **得点処理**: スコア加算 → 5点先取なら結果画面へ、そうでなければ次のサーブへ
5. **結果画面**: WIN / LOSE表示 → `R`でタイトルに戻る

## 勝利条件
- 自分が先に5点獲得する

## 失敗条件
- CPUが先に5点獲得する

## 難易度の上がり方
- ラリー中の得点合計（プレイヤー+CPU）が増えるほど以下を段階的に強化:
  - CPUの移動速度が上昇
  - CPUの反応（ボール落下地点の予測開始タイミング）が早くなる
  - ボールの初速・重力がわずかに増加し、ラリーのテンポが速くなる
- 具体的には合計得点が2点増えるごとに1段階（最大3段階）強化する

## 画面構成
- **キャンバスサイズ**: 800×450 (16:9)
- **タイトル画面**: ゲームタイトル、操作説明、「press any key」
- **プレイ画面**:
  - 上部中央: スコア表示 (`プレイヤー 3 - 2 CPU`)
  - 中央: 自陣後方からの固定カメラで見た3Dコート（透視投影、手前=自陣、ネット越しに相手コート）
  - 操作プレイヤー（緑色の円）、味方プレイヤー（青い円/カプセル、左側）、CPU（赤い円/カプセル、右側）
  - ボール（白い円、影付き）
- **結果画面**: WIN!/LOSE... の大きな文字、最終スコア、リスタート案内

## 必要なアセット
- 画像・音声アセットは使用しない（Canvas図形のみで表現し、外部依存をゼロにする）
- 色分けで役割を判別: 操作プレイヤー=緑、味方=青、CPU=赤、ボール=白、ネット=グレー、コート=緑系背景

## 技術スタック
- 素の HTML5 + CSS + JavaScript（ES Modules不使用、ビルド不要）
- 描画: Canvas 2D API
- ローカル起動: `python3 -m http.server` 等の簡易サーバー、またはブラウザで `index.html` を直接開く

## 実装順序
1. プロジェクト雛形作成（`index.html` / `style.css` / `main.js`）
2. コート描画（地面・ネット・背景）と固定カメラのゲームループ（requestAnimationFrame）
3. プレイヤーの移動・ジャンプ実装
4. ボールの物理演算（重力・反発・プレイヤー/ネット/地面との当たり判定）
5. CPU AI実装（ボール追従・打ち返し）
6. スコアリング・サーブ処理・勝敗判定
7. ゲーム状態管理（タイトル / プレイ中 / 結果）とUIオーバーレイ
8. 難易度スケーリングの組み込み
9. 動作確認・調整（当たり判定・難易度バランス）
